diff --git a/src/evaluate.h b/src/evaluate.h
--- a/src/evaluate.h
+++ b/src/evaluate.h
@@ -21,6 +21,7 @@
 
 #include <string>
 #include <optional>
+#include <utility>
 
 #include "types.h"
 
@@ -47,6 +48,7 @@ namespace Eval {
 
     std::string trace(Position& pos);
     Value evaluate(const Position& pos, bool adjusted = false);
+    std::pair<int, int> raw_evaluate(const Position& pos);
 
     void init();
     void verify();
diff --git a/src/nnue/evaluate_nnue.cpp b/src/nnue/evaluate_nnue.cpp
--- a/src/nnue/evaluate_nnue.cpp
+++ b/src/nnue/evaluate_nnue.cpp
@@ -136,6 +136,35 @@ namespace Stockfish::Eval::NNUE {
     return (bool)stream;
   }
 
+  // Evaluation function. Perform differential calculation.
+  std::pair<int, int> raw_evaluate(const Position& pos) {
+
+    constexpr uint64_t alignment = CacheLineSize;
+
+#if defined(ALIGNAS_ON_STACK_VARIABLES_BROKEN)
+    TransformedFeatureType transformedFeaturesUnaligned[
+      FeatureTransformer::BufferSize + alignment / sizeof(TransformedFeatureType)];
+    char bufferUnaligned[Network::BufferSize + alignment];
+
+    auto* transformedFeatures = align_ptr_up<alignment>(&transformedFeaturesUnaligned[0]);
+    auto* buffer = align_ptr_up<alignment>(&bufferUnaligned[0]);
+#else
+    alignas(alignment)
+      TransformedFeatureType transformedFeatures[FeatureTransformer::BufferSize];
+    alignas(alignment) char buffer[Network::BufferSize];
+#endif
+
+    ASSERT_ALIGNED(transformedFeatures, alignment);
+    ASSERT_ALIGNED(buffer, alignment);
+
+    const std::size_t bucket =
+      std::min((pos.count<ALL_PIECES>() - 1) * 8 / currentNnueVariant->nnueMaxPieces, 7);
+    const int psqt = featureTransformer->transform(pos, transformedFeatures, bucket);
+    const auto output = network[bucket]->propagate(transformedFeatures, buffer);
+    return {psqt, output[0]};
+  }
+
+
   // Evaluation function. Perform differential calculation.
   Value evaluate(const Position& pos, bool adjusted) {
 
diff --git a/src/uci.cpp b/src/uci.cpp
--- a/src/uci.cpp
+++ b/src/uci.cpp
@@ -389,6 +389,19 @@ void UCI::loop(int argc, char* argv[]) {
       else if (token == "bench")    bench(pos, is, states);
       else if (token == "d")        sync_cout << pos << sync_endl;
       else if (token == "eval")     trace_eval(pos);
+      else if (token == "horde-raw-eval")
+      {
+          Eval::NNUE::verify();
+          const auto [psqt, positional] = Eval::NNUE::raw_evaluate(pos);
+          sync_cout << "horde-raw-eval " << psqt << " " << positional << " "
+                    << psqt + positional << sync_endl;
+      }
+      else if (token == "horde-eval-debug")
+      {
+          Eval::NNUE::verify();
+          const Value value = Eval::evaluate(pos);
+          sync_cout << "horde-eval-debug eval=" << int(value) << sync_endl;
+      }
       else if (token == "compiler") sync_cout << compiler_info() << sync_endl;
       else if (token == "export_net")
       {
